// Package handler contains Fiber HTTP handlers: request parsing, response
// shaping, and status codes. Handlers delegate all business logic to the
// service layer and never talk to the database directly.
package handler
