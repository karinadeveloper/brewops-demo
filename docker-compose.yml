services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${DB_USER:-brewops}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-brewops}
      POSTGRES_DB: ${DB_NAME:-brewops}
    ports:
      - "${DB_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-brewops} -d ${DB_NAME:-brewops}"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
